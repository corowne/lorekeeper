/**
 * This is a secondary file handling dependencies which are non-critical
 * and can be deferred. However, it is still practical to handle them
 * via mix so that they can be processed at once and minified.
 */

require('./bootstrap4-toggle');
require('./lightbox');
